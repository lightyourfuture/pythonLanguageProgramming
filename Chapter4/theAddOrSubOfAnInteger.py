# 编写程序计算如下数列的值:
# 1-2+3-4...966
# 其中,所有数字为整数,从1开始递增,奇数为正,偶数为负
# 初始化总和为0
total_sum = 0

# 循环从1到966（包括）
for i in range(1, 967):
    # 如果是奇数，加上当前数字
    if i % 2 != 0:
        total_sum += i
    # 如果是偶数，减去当前数字
    else:
        total_sum -= i

print("数列的值是:", total_sum)
